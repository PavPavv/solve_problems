# Базовые алгоритмы и структуры данных

## Содержание

Алгоритмы:

1. Бинарный поиск (цикл или рекурсия)
2. Сортировка выбором
3. Быстрая сортировка
4. Сортировка слиянием
5. Пузырьковая сортировка
6. Поиск в ширину
7. Поиск в длинну

Структуры данных:

1. Массив (Список)
2. Хэш-таблица (Словарь)
3. Связанный список
4. Стэк (FILO)
5. Очередь (FIFO)
6. Граф
7. Девевья, как частный случай графа


## Алгоритмы

### 1. Бинарный поиск

#### Ключ к решению

"Разделяй и властвуй". Необходимо завести 3 переменные (_l_, _mid_ и _r_) и пересчитывать их в цикле **while** (с условием, которое поможет избежать бесконечного цикла) + добавить внутрь цикла условия (if/else if/ else) для пересчета _l_, _mid_ и _r_.

<details>
<summary>Подсказка</summary>

Ключевой момент - определение новой середины динамически:

```javascript
let middle = Math.floor((l + r) / 2);
```

</details>

<details>
<summary>📄 Решение</summary>

```javascript
function bSearch(arr, target) {
  const sortedArr = arr.sort((a, b) => a - b);
  let l = 0;
  let r = sortedArr.length - 1;
  let result = -1;

  while (l <= r) {
    let middle = Math.floor((l + r) / 2);
    let guess = sortedArr[middle];

    if (guess === target) {
      return result = middle;
    } else if (guess > target) {
      r = middle - 1;
    } else {
      l = middle + 1;
    }
  }
  return result;
}
console.log(bSearch([2,3,4,5,6,7,8,9,10], 9));
console.log(bSearch([2,3,4,5,6,7,8,9,10], -100));
```

</details>

#### Рекурсивное решение

<details>
<summary>Подсказка</summary>

Передать дополнительные параметры со значениями по умолчанию

```typescript
const recBSearch = (arr: number[], target: number, l = 0, r: number | null = null): number => {
  // code here
}
```

</details>

<details>
<summary>📄 Решение</summary>

```typescript
  const recBSearch = (arr: number[], target: number, l = 0, r: number | null = null): number => {
    if (r === null) {
      r = arr.length - 1;
    }
    if (l > r) return -1;

    let mid = Math.floor((l + r) / 2);

    if (arr[mid] === target) {
      return mid;
    } else if (arr[mid] > target) {
      return recBSearch(arr, target, l, mid - 1);
    } else {
      return recBSearch(arr, target, mid + 1, r);
    }
  };
  // console.log(recBSearch([3,2,4,5,6,8,7,9,10], 9));
  // console.log(recBSearch([2,3,4,5,6,7.7,8,9,10], -2));
```

</details>

### 2. Сортировка выбором

### 3. Быстрая сортировка

### 4. Сортировка слиянием

### 5. Пузырьковая сортировка

### 6. Поиск в ширину

### 7. Поиск в длинну

---
---

## Структуры данных

### 1. Массив (Список)

### 2. Хэш-таблица (Словарь)

### 3. Связанный список

### 4. Стэк (FILO)

### 5. Очередь (FIFO)

### 6. Граф

### 7. Девевья, как частный случай графа
